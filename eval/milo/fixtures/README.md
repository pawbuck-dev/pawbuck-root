# Sanitized document fixtures (optional)

Place **non-PII** PDF or image samples here for live Milo vision eval (`Category=MiloEvalLive`).

- Do not commit real owner/clinic identifiers.
- Prefer synthetic or redacted samples.
- Reference matching rows in `document-extraction/fixtures.json`.

Default CI uses JSON golden payloads only (no binary files required).
