# PawBuck.API default (dotnet run). Override in .env.local (gitignored).
VITE_ADMIN_API_BASE=http://localhost:5289
# Same Supabase project as the consumer app — optional for local dev if API allows anonymous support in Development.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
