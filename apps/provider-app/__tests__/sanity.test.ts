/**
 * Provider app (pet walker / pro) — expand with screen and service tests as flows land.
 * Jest uses ts-jest + node (see jest.config.js); add `jest-expo` UI tests when screens are ready.
 */
describe("provider app test harness", () => {
  it("runs Jest", () => {
    expect(1 + 1).toBe(2);
  });
});
