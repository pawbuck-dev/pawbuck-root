appId: com.pawbuck.app
name: PawBuck family & transfer E2E

env:
  MAESTRO_OWNER_EMAIL: ${MAESTRO_OWNER_EMAIL}
  MAESTRO_OWNER_PASSWORD: ${MAESTRO_OWNER_PASSWORD}
  MAESTRO_RECIPIENT_EMAIL: ${MAESTRO_RECIPIENT_EMAIL}
  MAESTRO_RECIPIENT_PASSWORD: ${MAESTRO_RECIPIENT_PASSWORD}
  MAESTRO_EMPTY_USER_EMAIL: ${MAESTRO_EMPTY_USER_EMAIL}
  MAESTRO_EMPTY_USER_PASSWORD: ${MAESTRO_EMPTY_USER_PASSWORD}
  MAESTRO_HOUSEHOLD_CODE: ${MAESTRO_HOUSEHOLD_CODE}
  MAESTRO_TRANSFER_CODE: ${MAESTRO_TRANSFER_CODE}
  MAESTRO_INVITE_TOKEN: ${MAESTRO_INVITE_TOKEN}
  MAESTRO_PET_NAME: ${MAESTRO_PET_NAME}
  MAESTRO_PET_EMAIL_LOCAL: ${MAESTRO_PET_EMAIL_LOCAL}
